# ==========================================
# LoRA fine-tuning dependencies (install in Colab)
# Kept separate from the main requirements.txt to avoid polluting the local environment
# ==========================================

# deep learning framework
torch>=2.2.0
accelerate>=0.28.0

# HuggingFace ecosystem
transformers>=4.40.0
datasets>=2.18.0

# LoRA / QLoRA fine-tuning
peft>=0.10.0
trl>=0.8.0
bitsandbytes>=0.43.0

# Qwen OpenAI-compatible API (Self-Instruct data expansion + LLM-as-Judge evaluation)
openai>=1.30.0

# utilities
tqdm>=4.66.0
